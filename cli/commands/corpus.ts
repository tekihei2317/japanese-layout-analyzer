export type CorpusOptions = {
  n?: string;
  count?: string;
};

export const corpusCommand = async (
  action: string,
  corpusId: string | undefined,
  options: CorpusOptions
) => {
  void action;
  void corpusId;
  void options;
  console.log("Not implemented");
};
