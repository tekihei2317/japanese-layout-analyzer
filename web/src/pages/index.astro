---
import BaseLayout from "../layouts/BaseLayout.astro";
import TopPageApp from "../components/TopPageApp";
import { loadMetricsData } from "../lib/metrics-data";

const { index, corpusDataById } = await loadMetricsData();
---

<BaseLayout title="Japanese Layout Analyzer">
  <TopPageApp client:load index={index} corpusDataById={corpusDataById} />
</BaseLayout>
